# TODO
- [ ] Update `styles.css` to add missing `--bg` variable so base background renders correctly.
- [x] Boost contrast/visibility for interactive UI: chips, primary/ghost buttons (hover/focus), FAQ question hover, and `mark` highlight.
- [x] Improve Category section visibility: stronger borders/backgrounds, darker panel behind controls, and a high-contrast selected state for category chips + select + option chips.
- [ ] Quick manual check: load `index.html` and confirm elements are clearly visible.

